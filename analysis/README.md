# Analysis

This subdirectory contains the code to automatically create comparisons of two (of the same) results files from subtasks of the babylm evaluation, so that the model can be evaluated against the baseline in more granularity than the final score.